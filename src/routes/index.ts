// import Elysia from 'elysia';
// const app = new Elysia();
// // postRoute.ts
// export const postsRoutes = (app: any) => {
//     app.get('/list', () => {
//       return { message: 'List of posts' };
//     });
  
//     app.get('/:id', ({ params }) => {
//       return { message: `Post with ID ${params.id}` };
//     });
//   };
  
//   // commentRoutes.ts
//   export const commentsRoutes = (app: any) => {
//     app.get('/list', () => {
//       return { message: 'List of comments' };
//     });
  
//     app.get('/:id', ({ params }) => {
//       return { message: `Comment with ID ${params.id}` };
//     });
//   };
  
  
//   // userRoutes.ts
//   export const userRoutes = (app: any) => {
//     app.get('/:id', ({ params }) => {
//       return { message: `User with ID ${params.id}` };
//     });
//   };
  
//   export default app